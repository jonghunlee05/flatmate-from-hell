import Phaser from 'phaser';

export default class DialogueBox extends Phaser.GameObjects.Container {
    constructor(scene: Phaser.Scene) {
        super(scene);
        // Dialogue box implementation will go here
    }
} 