import Phaser from 'phaser';

export default class HUD extends Phaser.GameObjects.Container {
    constructor(scene: Phaser.Scene) {
        super(scene);
        // HUD implementation will go here
    }
} 